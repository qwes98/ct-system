---
name: ct-issue-implement
description: GitHub 브랜치를 입력받아 worktree 생성 및 연결된 Issue를 읽고 구현 작업을 시작하는 스킬. 브랜치명 또는 git checkout 명령어를 파싱하여 자동으로 이슈 번호를 추출합니다.
triggers:
  - issue implement
  - branch implement
  - worktree
  - git checkout으로 시작하는 구현 요청
  - 브랜치명으로 이슈 구현 요청
---

# CT Issue Implement

GitHub 브랜치를 기반으로 worktree를 생성하고, 연결된 Issue를 읽어 구현 작업을 시작하는 스킬입니다.

## 입력 파싱

### 지원 형식

**형식 1: 브랜치명만**
```
infra/6-cicd-claude-code-review-action
```

**형식 2: git 명령어 포함**
```
git fetch origin
git checkout infra/6-cicd-claude-code-review-action에서-bot-트리거-시-실패-문제
```

### 파싱 규칙

1. `git checkout` 뒤의 브랜치명 추출 (공백/개행 기준으로 첫 번째 토큰)
2. 브랜치명에서 이슈 번호 추출:
   - 패턴: `{category}/{number}-{description}`
   - 정규식: `/(\d+)-` 패턴에서 첫 번째 매칭
   - 예: `infra/6-cicd-test` → 이슈 번호 `6`
3. worktree 경로 생성: `/`를 `-`로 치환
   - 예: `infra/6-cicd-test` → `../worktrees/infra-6-cicd-test`

## 워크플로우

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 입력 파싱                                                  │
│    - 브랜치명 추출                                            │
│    - 이슈 번호 추출                                           │
│    - worktree 경로 생성                                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Git Fetch                                                 │
│    git fetch origin                                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. Worktree 생성                                             │
│    git worktree add ../worktrees/{sanitized-name} origin/{branch}│
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. GitHub Issue 읽기                                         │
│    gh issue view {issue-number}                              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 도메인 스킬 호출                                           │
│    - frontend/ → ct-frontend                                 │
│    - backend/ → ct-backend                                   │
│    - infra/ → ct-infra                                       │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. Worktree 디렉토리에서 구현 작업 시작                         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. PR 생성 (⚠️ 제목 = Issue 제목과 동일)                       │
│    gh pr create --title "{Issue 제목 그대로}" --body "..."    │
└─────────────────────────────────────────────────────────────┘
```

## 실행 단계

### Step 1: 입력 파싱

사용자 입력에서 브랜치명과 이슈 번호를 추출합니다.

```bash
# 입력 예시
INPUT="git fetch origin
git checkout infra/6-cicd-claude-code-review-action"

# 브랜치명 추출 (git checkout 다음 토큰)
BRANCH=$(echo "$INPUT" | grep -oP 'git checkout \K[^\s]+' | head -1)
# 결과: infra/6-cicd-claude-code-review-action

# 이슈 번호 추출 (/ 다음 첫 번째 숫자)
ISSUE_NUM=$(echo "$BRANCH" | grep -oP '/\K\d+' | head -1)
# 결과: 6

# Worktree 경로 생성 (/를 -로 치환)
WORKTREE_PATH="../worktrees/$(echo "$BRANCH" | tr '/' '-')"
# 결과: ../worktrees/infra-6-cicd-claude-code-review-action
```

### Step 2: Git Fetch

```bash
git fetch origin
```

### Step 3: Worktree 생성

```bash
# 기존 브랜치를 추적하는 worktree 생성
git worktree add "$WORKTREE_PATH" "origin/$BRANCH"

# 또는 로컬 브랜치로 체크아웃
git worktree add "$WORKTREE_PATH" "$BRANCH"
```

**주의사항:**
- 동일 브랜치의 worktree가 이미 존재하면 에러 발생
- `git worktree list`로 기존 worktree 확인

### Step 4: GitHub Issue 읽기

```bash
gh issue view "$ISSUE_NUM"
```

Issue 내용을 확인하여 구현 요구사항을 파악합니다.

### Step 5: 도메인 스킬 호출

브랜치 prefix에 따라 적절한 도메인 스킬을 호출합니다:

| Prefix | 스킬 | 설명 |
|--------|------|------|
| `frontend/` | ct-frontend | Next.js + shadcn/ui 프론트엔드 |
| `backend/` | ct-backend | Spring Boot 백엔드 |
| `infra/` | ct-infra | Docker, CI/CD, 배포 |

### Step 6: 구현 작업 시작

Worktree 디렉토리로 이동하여 Issue 요구사항에 따라 구현을 진행합니다.

```bash
cd "$WORKTREE_PATH"
# 구현 작업 시작...
```

### Step 7: PR 생성 (⚠️ 중요)

**PR 제목은 반드시 Issue 제목과 동일하게 작성합니다.**

```bash
# 1. Issue 제목 가져오기
ISSUE_TITLE=$(gh issue view "$ISSUE_NUM" --json title -q '.title')

# 2. PR 생성 (Issue 제목 그대로 사용)
gh pr create --title "$ISSUE_TITLE" --body "$(cat <<'EOF'
## Summary
- 구현 내용 요약

## Related Issue
Closes #'"$ISSUE_NUM"'

## Test Plan
- [ ] 테스트 항목

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

## 예시

### 입력
```
git fetch origin
git checkout infra/6-cicd-claude-code-review-action에서-bot-트리거-시-실패-문제
```

### 파싱 결과
- **브랜치명**: `infra/6-cicd-claude-code-review-action에서-bot-트리거-시-실패-문제`
- **이슈 번호**: `6`
- **도메인**: `infra` → ct-infra 스킬
- **Worktree 경로**: `../worktrees/infra-6-cicd-claude-code-review-action에서-bot-트리거-시-실패-문제`

### 실행 순서
1. `git fetch origin`
2. `git worktree add ../worktrees/infra-6-cicd-... origin/infra/6-cicd-...`
3. `gh issue view 6`
4. ct-infra 스킬 참조
5. worktree에서 구현 시작
6. **PR 생성** (⚠️ 제목 = Issue #6 제목과 동일하게)

## PR 생성 규칙

### ⚠️ 중요: PR 제목은 반드시 Issue 제목과 동일해야 함

PR을 생성할 때 **PR 제목은 관련 Issue의 제목과 정확히 동일하게** 작성해야 합니다.

```bash
# Issue 제목 확인
gh issue view {issue-number} --json title -q '.title'

# PR 생성 시 동일한 제목 사용
gh pr create --title "{Issue 제목 그대로}" --body "..."
```

### PR 생성 절차

1. **Issue 제목 확인** (필수)
   ```bash
   ISSUE_TITLE=$(gh issue view {issue-number} --json title -q '.title')
   echo "Issue 제목: $ISSUE_TITLE"
   ```

2. **PR 생성** (Issue 제목을 그대로 사용)
   ```bash
   gh pr create \
     --title "$ISSUE_TITLE" \
     --body "$(cat <<'EOF'
   ## Summary
   - [구현 내용 요약]

   ## Related Issue
   Closes #{issue-number}

   ## Test Plan
   - [ ] 테스트 항목

   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   EOF
   )"
   ```

### 예시

**Issue #6 제목**: `CI/CD: Claude Code Review Action 추가`

**올바른 PR 제목**: `CI/CD: Claude Code Review Action 추가` ✅

**잘못된 PR 제목 예시**:
- `feat: add claude code review action` ❌
- `#6 구현` ❌
- `infra/6 작업 완료` ❌

### 근거

- PR과 Issue 간의 명확한 연결
- 일관된 프로젝트 히스토리 유지
- GitHub 자동 링크 및 검색 용이성

---

## Worktree 관리

### 목록 확인
```bash
git worktree list
```

### 작업 완료 후 제거
```bash
git worktree remove ../worktrees/{worktree-name}
```

### 강제 제거 (변경사항 있을 때)
```bash
git worktree remove --force ../worktrees/{worktree-name}
```

## 의존성

- **Git 2.5+**: worktree 기능 지원
- **gh CLI**: GitHub Issue 조회
- **ct-frontend / ct-backend / ct-infra 스킬**: 도메인별 구현 가이드

## 트러블슈팅

### Worktree가 이미 존재할 때
```bash
# 기존 worktree 확인
git worktree list

# 필요시 제거 후 재생성
git worktree remove ../worktrees/{name}
git worktree add ../worktrees/{name} origin/{branch}
```

### 브랜치가 로컬에 없을 때
```bash
# fetch 후 원격 브랜치로 직접 체크아웃
git fetch origin
git worktree add ../worktrees/{name} origin/{branch}
```

### 이슈 번호 추출 실패
브랜치 패턴이 `{category}/{number}-{description}` 형식이 아닌 경우:
- 사용자에게 이슈 번호를 직접 입력받음
- 또는 브랜치와 연결된 PR/Issue를 `gh` CLI로 검색

## 참조

- @references/worktree-guide.md - Git worktree 상세 가이드
